"use client";
import { useRef } from "react";
import Link from "next/link";
import { motion, useScroll, useTransform } from "framer-motion";
import { ArrowRight } from "lucide-react";

import {
  amazon_Logo,
  flipKart_Logo,
  myntra_Logo,
  nykaa_Logo,
  ajio_Logo,
} from "@/public/Ecommerce";
import Image from "next/image";

const BL = "#0167ff";
const GR = "#34c759";

const PLATFORMS = [
  { name: "Amazon", logo: amazon_Logo },
  { name: "Flipkart", logo: flipKart_Logo },
  { name: "Myntra", logo: myntra_Logo },
  { name: "Nykaa", logo: nykaa_Logo },
  { name: "Ajio", logo: ajio_Logo },
];

export default function PlatformSection() {
  const ref = useRef(null);
  const { scrollYProgress } = useScroll({ target: ref, offset: ["start end", "end start"] });
  const orbY = useTransform(scrollYProgress, [0, 1], [-30, 30]);

  return (
    <motion.div
      ref={ref}
      initial={{ opacity: 0, y: 40 }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true, margin: "-10% 0px" }}
      transition={{ duration: 0.8, ease: "easeOut" }}
      className="relative rounded-[2.5rem] overflow-hidden p-8 sm:p-14 bg-white border border-gray-50 shadow-[0_20px_50px_rgba(1,103,255,0.05)]"
    >
      {/* Background Orbs */}
      <motion.div
        className="absolute -top-24 -right-24 w-80 h-80 rounded-full blur-[100px] pointer-events-none opacity-20"
        style={{ background: `radial-gradient(circle, ${BL}, transparent)` }}
      />
      <motion.div
        className="absolute -bottom-24 -left-24 w-80 h-80 rounded-full blur-[100px] pointer-events-none opacity-20"
        style={{ background: `radial-gradient(circle, ${GR}, transparent)` }}
      />

      <div className="relative z-10 flex flex-col lg:flex-row items-center justify-between gap-12">
        <motion.div 
          initial={{ opacity: 0, x: -30 }}
          whileInView={{ opacity: 1, x: 0 }}
          transition={{ duration: 0.6, delay: 0.2 }}
          className="text-center lg:text-left max-w-sm"
        >
          <div className="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-[#34c759]10 border border-[#34c759]20 mb-4">
             <span className="w-1.5 h-1.5 rounded-full bg-[#34c759]" />
             <p className="text-[10px] font-bold uppercase tracking-widest text-[#34c759]">
              Multi-Platform Tracker
             </p>
          </div>
          <h3 className="text-3xl sm:text-4xl font-bold text-gray-900 leading-[1.1] mb-4">
            One search.<br /><span className="text-[#0167ff]">Every platform.</span>
          </h3>
          <p className="text-sm text-gray-500 leading-relaxed">
            We track prices across all major shopping platforms instantly so you always get the best deal.
          </p>
        </motion.div>

        <motion.div 
          initial="hidden"
          whileInView="show"
          variants={{
            show: {
              transition: {
                staggerChildren: 0.08,
                delayChildren: 0.3
              }
            }
          }}
          className="flex flex-wrap items-center justify-center lg:justify-start gap-4"
        >
          {PLATFORMS.map((p, i) => (
            <motion.div
              key={i}
              variants={{
                hidden: { opacity: 0, scale: 0.9 },
                show: { opacity: 1, scale: 1 }
              }}
              whileHover={{ y: -5, scale: 1.05 }}
              className="flex flex-col items-center gap-4 p-6 rounded-3xl bg-gray-50/50 border border-gray-100/50 transition-all hover:bg-white hover:shadow-xl hover:shadow-blue-500/5"
            >
              <div className="w-14 h-14 rounded-2xl bg-white shadow-sm flex items-center justify-center p-3">
                <Image src={p.logo} alt={p.name} width={40} height={40} className="w-full h-full object-contain" />
              </div>
              <span className="text-[10px] font-bold text-gray-400 uppercase tracking-widest">{p.name}</span>
            </motion.div>
          ))}
        </motion.div>

        <Link href="/how-it-works">
          <motion.div
            initial={{ opacity: 0, scale: 0.8 }}
            whileInView={{ opacity: 1, scale: 1 }}
            transition={{ delay: 0.6, duration: 0.4 }}
            className="relative px-8 py-4 rounded-2xl font-bold text-sm text-white overflow-hidden shadow-[0_10px_25px_rgba(1,103,255,0.25)] hover:shadow-[0_15px_30px_rgba(1,103,255,0.35)] transition-shadow"
            whileHover={{ scale: 1.05 }}
            whileTap={{ scale: 0.98 }}
            style={{ background: "#0167ff" }}
          >
            <span className="relative z-10 flex items-center gap-2.5">
              How It Works <ArrowRight className="w-4 h-4" />
            </span>
          </motion.div>
        </Link>
      </div>
    </motion.div>
  );
}
